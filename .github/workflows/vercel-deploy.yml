name: Deploy to Vercel

on:
  push:
    branches: [main] # Change this if your production branch is named differently

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel Deploy Hook
        run: curl -X POST ${{ secrets.VERCEL_DEPLOY_HOOK }}
